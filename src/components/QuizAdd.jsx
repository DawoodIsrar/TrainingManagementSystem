import axios from 'axios'
import React from 'react'
import { Button, Form } from 'react-bootstrap'

const QuizAdd = ({ getAllQuizes, selectedCouserId, setQuizName, quizName }) => {
  return <></>
}

export default QuizAdd
